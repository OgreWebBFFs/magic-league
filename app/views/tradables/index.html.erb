
<div id="user-wrapper" class="container">
<h3 class="color-blocked dark centered">Users</h3>
<div class="centered content">

<ul>
  <% @users.each do |user| %>
    <li>
     <%= link_to user.name, wishlist_path(user.id), :class => "btn user" %>
    </li>
  <% end %>
</ul>
</div>
</div>