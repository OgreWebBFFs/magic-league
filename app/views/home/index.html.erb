<%= react_component("CyclingBackground", {bgPath:get_random_bg_image}) %>
<% draffle = Draffle.all.first %>
<%= react_component("Rankings", {
  date: @match_date,
  rankedPlayers: @ranked_players,
  unrankedPlayers: @unranked_players,
  draffle: draffle.id
}) %>
